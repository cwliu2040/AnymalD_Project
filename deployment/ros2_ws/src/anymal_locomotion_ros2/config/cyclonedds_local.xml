<?xml version="1.0" encoding="UTF-8" ?>
<CycloneDDS
  xmlns="https://cdds.io/config"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="https://cdds.io/config https://raw.githubusercontent.com/eclipse-cyclonedds/cyclonedds/master/etc/cyclonedds.xsd"
>
  <Domain Id="any">
    <General>
      <!-- Allow standard ROS 2 terminals to discover this graph through SPDP.
           User data still uses unicast or Iceoryx shared memory. -->
      <AllowMulticast>spdp</AllowMulticast>
    </General>
    <Discovery>
      <ParticipantIndex>auto</ParticipantIndex>
      <MaxAutoParticipantIndex>50</MaxAutoParticipantIndex>
      <Peers>
        <Peer Address="127.0.0.1" />
      </Peers>
    </Discovery>
    <!--
      Multi-megabyte local scans use Iceoryx instead of fragmented UDP. Keep
      CycloneDDS's normal UDP fragment size for small topics and remote peers.
    -->
    <SharedMemory>
      <Enable>true</Enable>
      <LogLevel>warn</LogLevel>
    </SharedMemory>
  </Domain>
</CycloneDDS>
